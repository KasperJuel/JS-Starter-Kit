/* This is used by webpack in production build only.
  All libraries referenced here will be written to vendor.js
  so they can be cached until one change.
  Avoids users having to download a huge js file anytime a line
  of code changes.
  */

/* eslint-disable no-unused-vars */

import fetch from 'whatwg-fetch'
